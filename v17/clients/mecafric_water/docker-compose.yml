# Auto-généré par migration/from-openensdev/04_migrate_client.py
# Client : mecafric_water  (Odoo 17)  port host : 8076
# v17 : ens_core bind externe vers pacadev/modules/ens_core-17/.
#
# Prérequis :
#   - infra pacadev démarrée : pacadev infra start
#   - DB 'mecafric_water_prod' existante dans pacadev_postgres_shared
#   - Secrets injectés via : eval $(sops -d core/secrets/mecafric_water.enc.yaml | yq ...)
#
services:
  mecafric_water_odoo:
    build:
      context: ../../
      dockerfile: Dockerfile
    container_name: mecafric_water_odoo
    environment:
      HOST: pacadev_postgres_shared
      POSTGRES_DB: mecafric_water
      PORT: "5432"
      USER: odoo
      PASSWORD: odoo
    volumes:
      - ./addons/oca:/mnt/extra-addons/oca:ro
      - ./addons/ens_core:/mnt/extra-addons/ens_core:ro
      - /home/pacadev/pacadev/modules/ens_core-17:/mnt/extra-addons/ens_core_shared:ro
      - ./config:/etc/odoo
      - ./filestore:/var/lib/odoo/filestore
    ports:
      - "8076:8069"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8069/web/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - pacadev-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mecafric_water.rule=Host(`mecafric_water.pacadev.local`)"
      - "traefik.http.routers.mecafric_water.entrypoints=web"
      - "traefik.http.services.mecafric_water.loadbalancer.server.port=8069"

networks:
  pacadev-network:
    external: true
